NEXT_PUBLIC_API_URL="http://localhost:4000/api"
NODE_ENV="development"

# JWT secret — MUST match the api/.env JWT_SECRET exactly.
# Used by Next.js Edge Middleware to verify JWT signatures.
# This is a server-side variable (no NEXT_PUBLIC_ prefix) so it is
# never exposed to the browser.
JWT_SECRET="maleo_dev_jwt_secret_2026_change_me"
